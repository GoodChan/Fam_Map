package Shared.Server.Handlers;

import javax.xml.ws.spi.http.HttpExchange;

import static org.junit.Assert.*;

public class IndexHandlerTest {
    IndexHandler ih = new IndexHandler();
    //ih.handle(new HttpExchange);
}